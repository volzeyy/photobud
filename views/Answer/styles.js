import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  main: {
    display: "flex",
    flex: 1,
    marginTop: 15,
    marginBottom: 15,
  },

  answerContainer: {
    display: "flex",
    flex: 1,
    gap: 15,
    marginBottom: 15,
  },
});

export default styles;
