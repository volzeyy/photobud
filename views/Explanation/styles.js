import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  main: {
    marginTop: 15,
    paddingBottom: 15,
  },

  answerContainer: {
    display: "flex",
    gap: 15,
  },
});

export default styles;
